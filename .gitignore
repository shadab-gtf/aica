node_modules/
dist/
out/
build/
.next/
coverage/
*.tsbuildinfo
.turbo/
.vscode-test/
*.vsix

# local state / secrets
# .mcp.json holds MCP server credentials in plain text (e.g. the Context7 API
# key as an Authorization header), so it stays out of the repository.
.mcp.json
.claude/settings.local.json
.env
.env.*
!.env.example
.aica/
*.db
*.db-journal
*.db-wal
*.db-shm

# os
.DS_Store
Thumbs.db
desktop.ini

# next / playwright build output
.next/
playwright-report/
test-results/
